# PR 待ちブランチ — origin に push 済み、PR はまだ出していない修正

- **最終更新**: 2026-09-27, 3（候補 8 番 #4001「古い UI でジョブを編集するとローカルフォルダの保存先が表示されない」を直して `fix/local-folder-shown-on-edit` を push（**master 直上で独立**、PR はまだ、`Fixes #4001`）。#4405・#3750 と同じ `EditUriBuiltins.js` だが別の箇所。新しい UI は影響なし（実測）。open は 9 本のまま）
- 2026-09-27, 2（候補 7 番 #3750「古い UI で S3 の独自のストレージクラスが保存されない」を直して `fix/s3-custom-storage-class` を push（**master 直上で独立**、PR はまだ、`Fixes #3750`）。#4405 のブランチと同じファイルだが別の箇所で、衝突しない。新しい UI は影響なし（実測）。open は 9 本のまま）
- 2026-09-27（候補 6 番 #4405「古い UI で S3 のクライアントの設定が aws に戻る」を直して `fix/s3-client-kept-on-edit` を push（**master 直上で独立**、PR はまだ、`Fixes #4405`）。新しい UI は影響なし（実測）。open は 9 本のまま）
- 2026-09-26, 25（候補 5b（Issue なし、#3483 の再現中に発見）「複数ドライブのバックアップでルートの一覧が 1 ドライブ約 30 秒、空のルートが余計に出る」を直して `fix/list-prefix-multiple-roots` を push（**master 直上で独立**、PR はまだ）。新しい UI は既定では v2 の list-folder を使うので影響なし、v2 を無効にしたときだけ影響（実測）。open は 9 本のまま）
- 2026-09-26, 24（候補 5 番 #3483「古い UI の restore で、展開前のルートにチェックを付けられない」を直して `fix/restore-picker-unexpanded-root` を push（**master 直上で独立**、PR はまだ、`Fixes #3483`）。再現の途中で、別件「複数ドライブのバックアップはルートの一覧に 1 ドライブ約 30 秒かかり、空のルートが 1 つ余計に出る」を見つけた（未修正、レポート 09-26, 27 の項）。open は 9 本のまま）
- 2026-09-26, 23（候補 4 番 #6837「バックアップが終わってもスリープしない」を直して `fix/sleep-prevention-dedicated-thread` を push（**master 直上で独立**、PR はまだ、`Fixes #6837`）。open は 9 本のまま）
- 2026-09-26, 22（#7330 の作業中に見つけた「終了時に `QueueRunnerService.Terminate` が `ObjectDisposedException`」を直して `fix/queue-runner-dispose-before-terminate` を push（**master 直上で独立**、PR はまだ、Issue なし）。open は 9 本のまま）
- 2026-09-26, 21（候補 1 番 #7330「除外ポートにかかると次のポートに移らず落ちる」を直して `fix/server-port-refused-tries-next` を push（**master 直上で独立**、PR はまだ、`Fixes #7330`）。open は 9 本のまま）
- 2026-09-26, 20（候補 3 番（Issue なし、#7366 の続き）「派生の DB の初期化の失敗で接続が残る」を直して `fix/local-database-derived-setup-closes-connection` を push（**#7366 の上に積んだ**、PR はまだ）。open は 9 本のまま）
- 2026-09-26, 19（候補 2 番（Issue なし）「修復が健全な DB をリネームする」を再現して直し、`fix/repair-abort-keeps-database` を push（**master 直上で独立**、PR はまだ）。open は 9 本のまま）
- 2026-09-26, 18（候補 #5572「ライブログが行を飛ばす」を直して `fix/live-log-poll-continues-from-oldest` を push（**master 直上で独立**、PR はまだ、`Fixes #5572`）。open は 9 本のまま）
- 2026-09-26, 17（#7367 に `Failed` 報告のパス伏せを含めて 1 コミットのまま更新・push、PR 本文も更新。open は 9 本のまま）
- 2026-09-26, 16（`fix/missing-destination-no-retry` を [#7368](https://github.com/duplicati/duplicati/pull/7368) として出した。PR 本文の「DB が開いたまま」の注記を #7366 への参照に更新。open は 9 本（#7344 #7359 #7362 #7363 #7364 #7365 #7366 #7367 #7368））
- 2026-09-26, 15（`fix/http-report-status-result-errors` を [#7367](https://github.com/duplicati/duplicati/pull/7367) として出した。同じ症状の Issue はなし。open は 8 本（#7344 #7359 #7362 #7363 #7364 #7365 #7366 #7367））
- 2026-09-26, 14（5 番「HttpReportStatus の完了報告が例外だけで決まる」を直して `fix/http-report-status-result-errors` を push（**master 直上で独立**、PR はまだ）。open は 7 本のまま）
- 2026-09-26, 13（`fix/local-database-open-failure-closes-connection` を [#7366](https://github.com/duplicati/duplicati/pull/7366) として出した。同じ症状の Issue はなし。open は 7 本（#7344 #7359 #7362 #7363 #7364 #7365 #7366））
- 2026-09-26, 12（「失敗したバックアップの DB が開いたまま残る」を直して `fix/local-database-open-failure-closes-connection` を push（**master 直上で独立**、PR はまだ）。open は 6 本のまま）
- 2026-09-26, 11（4 番「保存先がないと再試行で約 50 秒待つ」を直して `fix/missing-destination-no-retry` を push（**master 直上で独立**、PR はまだ出していない）。方針は利用者が選択: バックアップ以外は即失敗、バックアップは従来どおり再試行。open は 6 本のまま）
- 2026-09-26, 10（3 番の調査の結果、止める挙動は意図した設計なので、エラーメッセージだけを改善した `fix/restore-priority-file-failure-message` を push。**PR は出さない**。B の上に積んだ。open は 6 本のまま）
- 2026-09-26, 9（気付き A「優先ファイルを諦める 2 経路がバリアを fault させずハング」を `fix/restore-priority-file-skip-releases-waiters`、B「`CopyOldTargetToNew` の失敗だけロックなしで `BrokenLocalFiles` に追加」を `fix/restore-copy-failure-takes-lock` として push。**PR は出さない**。積み順は #7364 → skipped-file-releases-blocks → report-empty-file-failure → A → B。open は 6 本のまま）
- 2026-09-26, 8（気付き 2 番「空ファイルを作れなくても `BrokenLocalFiles` に入らない」を直して `fix/restore-report-empty-file-failure` を push。**PR は出さない**。**1 番のブランチの上に積んだ**（同じ catch を触るため）— #7364 → 1 番 → 2 番 の順にマージ後、rebase して出す。open は 6 本のまま）
- 2026-09-26, 7（前回の気付き 1 番「スキップしたファイルのブロック数を戻さない」を直して `fix/restore-skipped-file-releases-blocks` を push。**PR は出さない**（依頼）。**#7364 のコミットの上に積んだ** — #7364 のマージ後に master へ rebase して PR にする。open は 6 本のまま）
- 2026-09-26, 6（#4644（失敗した restore に「成功」表示）を直して `fix/task-state-reports-result-errors` → [#7365](https://github.com/duplicati/duplicati/pull/7365)、**`Fixes #4644` と `Fixes #4051`**（同じ症状、GitHub も両方認識）。open は 6 本（#7344 #7359 #7362 #7363 #7364 #7365））
- 2026-09-26, 5（実装候補 2 番目「restore で 1 ファイルの失敗が file processor ごと落とす」（Issue なし）を直して `fix/restore-continue-after-file-failure` → [#7364](https://github.com/duplicati/duplicati/pull/7364)。関連: 閉じた [#6845](https://github.com/duplicati/duplicati/issues/6845)（メタデータ側の同じ形、#6966 で修正済み）。open は 5 本（#7344 #7359 #7362 #7363 #7364））
- 2026-09-26, 4（#4159（Web UI のコマンドラインの `delete --version` が別の版を消す）を直して `fix/commandline-pin-version` → [#7363](https://github.com/duplicati/duplicati/pull/7363)、`Fixes #4159` 付き。open は 4 本（#7344 #7359 #7362 #7363））
- 2026-09-26, 3（#3123 の残り（空・空白だけの引数が消える）を直して `fix/wrap-empty-commandline-argument` → [#7362](https://github.com/duplicati/duplicati/pull/7362)、`Fixes #3123` 付き。open は 3 本（#7344 #7359 #7362））
- 2026-09-26, 2（master がカナリア 2.4.0.102 のリリース（#7361）に進み、**利用者が** #7344・#7359・`triage` を master `6ed3c678` に rebase して push。rebase の前後で差分の中身が同じこと、どちらの PR も利用者の 1 コミットのままであることを確認。CI は両方とも実行中）
- 2026-09-26（依頼で `FileProcessor` を調べ、④ を #7359 に畳んで master `07e46d9e` に rebase・push、PR 本文も更新。#7344 も rebase・push（どちらもコミットは利用者のみと確認してから）。**#7336 は閉じられた** — メンテナが自分の #7274 に取り込んだ。open は 2 本（#7344 #7359））
- 2026-09-25, 4（#7345 が**マージ**（無改変）。#7359 に volume consumer の修正を畳んで rebase・push、PR 本文も更新。#7344 は rebase・push。**#7336 は rebase しない** — メンテナ（kenkendk）がこのブランチに master をマージするコミット `559a8d0f` を push した（#7329 のマージ直後、`HyperVSourceProvider`/`MSSQLSourceProvider`/テストの追随込み）。force-push するとそれを消すので、以後 #7336 はメンテナの作業として触らない。open は 3 本（#7336 #7344 #7359））
- 2026-09-25, 3（`VolumeDecompressor` を調べて残り 2 経路を見つけ、`fix/restore-stage-exit-releases-writers` → [#7359](https://github.com/duplicati/duplicati/pull/7359)。open は 4 本（#7336 #7344 #7345 #7359）。経緯はレポート 09-25, 2 の項）
- 2026-09-25, 2（#7346 が**マージ**（無改変）→「マージ済み」へ。open は 3 本（#7336 #7344 #7345）、master `89435a56` に rebase して push、**conflict なし**）
- 2026-09-25（#7357 と #7347 が**マージ**（どちらも無改変）→ 下の「マージ済み」へ。open は 4 本（#7336 #7344 #7345 #7346）。4 本＋`triage` を master `64adb826` に rebase。**#7345 だけ `DirectListHandlerTests.cs` で conflict** — #7347 と同じ位置にテストを 1 本ずつ足していただけなので両方残して解消、`DirectListHandlerTests` 18/18 緑、ブランチの差分は元のコミットと同じ（+35 −1）。push 済み）
- 2026-09-24（#7357 に 3 か所を追加して force-push、PR 本文も更新。`DownloadRequest` を調べたら、同じ「止まった段の上流の書き手が待ち続ける」形が downloader・decryptor・`VolumeManager` の `VolumeResponse` にもあった。経緯はレポート 09-24 の項）
- 2026-09-23, 2（**PR 未作成は 0 本**。`fix/restore-cancel-late-block-waiters` の残りのデッドロックを突き止めて同じ 1 コミットに畳み、[#7357](https://github.com/duplicati/duplicati/pull/7357) として出した。経緯はレポート 09-23, 2 の項）
- 2026-09-23（**自分の PR が 6 本マージ**（#7324 #7325 #7326 #7341 #7342 #7343、全部無改変）→ 下の「マージ済み」へ。open は 5 本（#7336 #7344 #7345 #7346 #7347）＋ PR 未作成 1 本。残り 6 ブランチ＋`triage` を master `fa55d270` に rebase して push（**衝突なし** — 一覧系 4 本は `DirectListHandlerTests.cs` で衝突すると見込んでいたが、#7326 のマージ後も文面上は当たらなかった）。`fix/restore-test-corrupt-data-volume` は**不要になった** — メンテナの #7355 が同じフレークを同じ方法で直したので、rebase で conflict になったローカルは abandon）
- 2026-09-22, 2（新規 2 本を push、**PR は出していない**（利用者の指示）: `fix/restore-test-corrupt-data-volume`（restore-test のフレーク修正）と `fix/restore-cancel-late-block-waiters`（壊れた volume で restore が止まる件の一機構、完全修正ではない）。経緯はレポート 09-22 の項）
- 2026-09-22（master が 4 マージ進んだので 11 ブランチ＋`triage` を master `34f6c753` に rebase して push（衝突なし）。09-21 の PR の CI 赤は 2 種類 — **[#7343](https://github.com/duplicati/duplicati/pull/7343) の macOS 赤は自分のテストの欠陥**（2 回のバックアップが 1 秒差だと purge が「次の fileset より後の時刻になる」で止まる。最初の fileset 時刻＋2 秒まで待つように修正、単一コミットに畳んだ）、もう 1 つ `RestoreTestHandlerTests.CorruptedDblockIsReportedWithPathsAndErrorExitCodeAsync` の macOS 赤は master 側のフレーク（詳細はレポート 09-22 の項））
- 2026-09-21（**PR 待ちは 0 本**。#7323 が**マージ**（無改変、[`a2c14630`](https://github.com/duplicati/duplicati/commit/a2c14630ba60)）→ 表から外した。ローカルの全ブランチ（11 本、#7336 の `feature/hyperv-mssql-source-providers` 含む）を master `e771d8b1` に rebase して push（11 本とも衝突なし）、残っていた 7 本を PR に：#7341 #7342 #7343 #7344 #7345 #7346 #7347。open な PR は自分のもので **11 件**。一覧系 4 本＋fileset 完全一致（#7342 #7344 #7345 #7346 #7347）と #7326 は全部 `DirectListHandlerTests.cs` を触るので、**1 本マージされるごとに残りが conflict になる** → その都度 master に rebase して push。#7342 と #7343 は `SetLocksHandlerTests.cs` を共有、同様）
- 2026-09-16: 4 本を PR に（#7323 #7324 #7325 #7326）
- 2026-09-15: 8 本を master `343d00c4` に rebase して push、この表を作成
- **方針**: open な PR が多い間は新しい PR を出さず、修正だけ origin（`JamBalaya56562/duplicati`）に push しておく。
  古い PR がマージされたら、ここから順に PR を出す
- **PR 本文の下書き**: [`pr-drafts/`](pr-drafts/)（英語、**修正した Issue があれば `Fixes #N` を付ける**（2026-09-26 から。#7278 が #3123 に付け忘れたため）、一部だけ直す場合は付けずに本文で範囲を書く、末尾に 🤖 行）。ブランチ名の `fix/` を除いた名前で 1 ファイル
- **PR の出し方**: `gh pr create -R duplicati/duplicati --head JamBalaya56562:<bookmark> --title "<コミット 1 行目>" --body-file triage/pr-drafts/<name>.md`。
  出したらこの表の「PR」欄を埋め、下書きは残す（本文の修正は GitHub 側で）
- **CI**: PR を開くまで CI は走らない。rebase 後はローカルでビルド・テストを**していない**（利用者の指示：CI で赤が出たら見る）。09-22 の #7343 のテスト修正だけは Windows で 5/5 を実測

## 一覧（全部 master `6ed3c678`（09-26、canary 2.4.0.102）直上の 1 コミット。全部 PR 済み）

| ブランチ | commit | 内容 | 赤→緑（実測済み） | PR |
|---|---|---|---|---|
| `fix/root-listing-file-prefix` | d6c0fd7e | ルート一覧が、ファイルソースの名前を延ばした別ソース（`notes.txt` と `notes.txt.old`）を落とす → 復元ツリーに出ない。フォルダ（末尾区切り）のルートだけが配下を吸収 | Win + WSL 2 件、15/15 | [#7344](https://github.com/duplicati/duplicati/pull/7344)（09-21） |
| `fix/restore-stage-exit-releases-writers` | 31feb863 | #7357 の残り 4 経路（どれも「止まった段の上流の書き手が待ち続ける」形）。① decompressor が 1 つだけ（CPU 2〜3 コアの既定）だと失敗時の `Retire()` を読み切る者がいない。② downloader・decryptor・decompressor が `catch (RetiredException)` で抜けると入力を**まったく** retire しない。③ BlockManager の volume consumer の失敗時が `Retire()`。④ `FileProcessor` が抜けるとき応答チャネルが `Retire()` → 先読みした要求に答え続ける block handler が停止。すべて即時 retire に | Win: ①② 展開失敗で master 1 周目停止 → 4 構成 20/20。③ consumer に例外注入で 8 周目停止 → 5 構成 20/20。④ FP に例外注入で FP 4・バッファ 4 が 3 周目停止 → 6 構成 20/20。どれも結果は `Error` と正しく報告。対象 3 クラス 79/79 は ①② の版のみ、③④ と rebase 後はローカル未実行（利用者の指示で CI に任せた） | [#7359](https://github.com/duplicati/duplicati/pull/7359)（09-25、③④ を後から追加） |
| `fix/wrap-empty-commandline-argument` | 72bf556a | #3123 の残り（#7278 が手を付けなかった点）。`WrapCommandLineElement` が空・空白だけの引数を引用せずに返していた（`string.IsNullOrWhiteSpace(arg) ⇒ return arg`）→ 空の引数は消え、空白だけの引数は区切りに溶けて消える。サービス登録では空の引数が落ち、エクスポートでは空白だけの値が `--name= ` になって値が消える。空は `""`、空白だけは通常の経路で引用、null は null のまま | Win: `CommandLineWrappingTests` 直す前 13 赤 / 35 緑 → 直した後 48 緑。エクスポートの `--name=` + 値の形も `CommandLineToArgvW` で読み戻し（空白 1 文字だけが赤だった）。bash でも `""`・`" "`・`--passphrase=" "` が元どおり | [#7362](https://github.com/duplicati/duplicati/pull/7362)（09-26、**`Fixes #3123`**） |
| `fix/commandline-pin-version` | 6b1dbdcb | #4159: Web UI のコマンドラインから送ったコマンドはキューで待ち、`--version` の番号（新しい順の位置）は実行時に解釈される → 先に定期バックアップが走ると番号が 1 つずれ、`delete --version=1` が**別の版を消す**。送った時点で DB（読み取り専用、操作のロックは取らない）から番号を版の時刻に固定し、実行時に今の番号へ付け替える。固定した版が消えていたら実行しない | Win: `CommandlineVersionPinningTests`（新規、`StartTask` → 保持するキュー → 間にバックアップ → タスク実行）。直す前 3 赤 / 1 緑 → 直した後 4 緑 | [#7363](https://github.com/duplicati/duplicati/pull/7363)（09-26、**`Fixes #4159`**） |
| `fix/restore-continue-after-file-failure` | 79050d72 | restore で 1 ファイルの作成・書き込みが失敗すると、その file processor が rethrow して止まる → FP 1 つ（2〜3 コアの既定）なら restore 全体がその例外で失敗し、後続ファイルは 1 つも戻らない。1 ファイルの失敗は報告して次へ進む（RetiredException は従来どおり終了）。先読み要求の未読応答を読み捨て（しないと次のファイルが取り違え、FP と BlockManager が待ち合って**ハング**）、使わないブロック（メタデータ含む）を CacheEvict で解放（しないと `BlockCountError`） | Win + Linux(WSL ext4): `RestoreFileFailureTests`（新規 2 件: ファイルの位置にフォルダ / DB で長さ 1 PiB + `restore-preallocate-size`）。直す前 2 赤 → 直した後 2 緑。変異: 読み捨てなし → 2 件目が 2 分でタイムアウト、解放なし → 両方 `BlockCountError 21`、メタデータ解放なし → `1` | [#7364](https://github.com/duplicati/duplicati/pull/7364)（09-26、Issue なし） |
| `fix/task-state-reports-result-errors` | f37e2cae | #4644 / #4051: 例外を投げずに終わったタスクは、結果にエラーがあっても `Completed`・`ErrorMessage` なしと報告される（`QueueRunnerService` が `Runner.RunAsync` の結果を捨てていた）。ngclient の restore 画面は `Completed && ErrorMessage == null` で成功と判定するので、ファイルを 1 つも戻せなかった restore が「成功」になる。`ParsedResult` が Error/Fatal なら `CachedTaskResult.ErrorMessage` に残し、`TaskQueueService` が例外がないときの `ErrorMessage` として返す。`Status` は `Completed` のまま（クライアントは Completed/Failed で終了を判定）。ngclient 側の変更は不要 | Win + Linux(WSL ext4): `ServerApiIntegrationTests.RestoreWithErrorsReportsAnErrorMessage_Async`（新規、読めないファイルが restore 先にある）直す前 赤 → 直した後 緑。`RestoreFromAMissingDestinationReportsFailure_Async`（新規、Issue の元の状況＝保存先なし）は master でも緑＝番人（Win）。クラス全体＋`TaskQueueServiceTests` 20 緑 | [#7365](https://github.com/duplicati/duplicati/pull/7365)（09-26、**`Fixes #4644` `Fixes #4051`**） |
| `fix/restore-skipped-file-releases-blocks` | 4a86e279 | **#7364（`79050d72`）の上に積んだ**。restore がスキップしたファイル（対象ファイルの確認失敗、ブロック数の不一致、空ファイルの作成失敗、ボリューム ID が負）がブロック数を戻さず、終了時に `BlockCountError`/`VolumeCountError` が出る。日付付きのコピーが既にある場合（FileAlreadyExists）は失敗ですらないのにエラー 2 件。スキップ時に #7364 の `ReleaseUnusedBlocksAsync` で解放し、FileAlreadyExists は missing を verified に移して解放。`RestoreOtherProcessIsUsingFileAsync` の「エラー 4 件」は 2 件に | Win + Linux(WSL ext4): `RestoreFileFailureTests` に新規 4 件。直す前 4 赤（`BlockCountError` 21 / 1 / 21 / 20）→ 直した後 4 緑。restore 関連の既存 19 件は緑（更新した 1 件を含む） | —（#7364 のマージ後に rebase して出す。本文は `pr-drafts/restore-skipped-file-releases-blocks.md`） |
| `fix/restore-report-empty-file-failure` | c4c63b44 | **1 番（`4a86e279`）の上に積んだ**。空ファイルの作成に失敗しても、ブロック数の不一致でも、エラーログだけで `BrokenLocalFiles` に入らず、最後の「Failed to restore N local files」から漏れる。両方を lock 付きで追加。空ファイルの catch は他と同じく stop/abort を通す（中断でファイルを失敗扱いしない） | Win + Linux(WSL ext4): `RestoreFileFailureTests.AnEmptyFileThatCannotBeCreatedIsReported`（新規）直す前 赤（一覧が空）→ 直した後 緑。同クラス 7 件・restore 関連の既存 19 件は緑 | —（#7364 → `fix/restore-skipped-file-releases-blocks` のマージ後に rebase して出す。本文は `pr-drafts/restore-report-empty-file-failure.md`） |
| `fix/restore-priority-file-skip-releases-waiters` | 131b6353 | **2 番（`c4c63b44`）の上に積んだ**。空ファイルの作成失敗とボリューム ID が負の経路は、優先ファイルでも優先バリアを fault させず、残り数も減らさない → 他のファイルが中断まで待ち続ける（FP 1 つなら 0 件復元）。両方で `FaultPriorityBarrierIfPriorityFile` を呼ぶ。優先ファイルはコールバックモジュールが加えられる | Win + Linux(WSL ext4): `RestoreFileFailureTests` に新規 2 件（優先ファイルを付けるモジュール）。直す前は 2 件とも 2 分の上限で赤（ハング）→ 直した後は数秒で緑。直す前の赤はそれぞれ単独で実行（中断した restore が次のテストのバックアップを壊すため）。クラス 9 件と関連 19 件が緑 | —（前のブランチのマージ後に rebase。本文は `pr-drafts/restore-priority-file-skip-releases-waiters.md`） |
| `fix/restore-copy-failure-takes-lock` | 43a50bc7 | **A の上に積んだ**（1 か所だけなので master 単独でも可）。`CopyOldTargetToNew` の失敗だけ `BrokenLocalFiles.Add` にロックなし。`FileBackedStringList` はスレッドセーフでない（2 スレッド×2000 件の同時追加で 20/20 回壊れる: Add が例外、読むと `OverflowException`）。他と同じ `lock (results)` を取る | 赤→緑のテストは作れない（衝突のタイミング）。根拠は同時追加の実測。関連テスト 28 件が緑 | —（本文は `pr-drafts/restore-copy-failure-takes-lock.md`） |
| `fix/restore-priority-file-failure-message` | a7725fb0 | **B（`43a50bc7`）の上に積んだ**。優先ファイルの失敗で restore が止まるのは意図した設計（DiskImage のレイアウトを先に作るため、`c780367c5`）。ただし利用者に見えるのはそのファイルの生の例外だけで、restore を止めた理由が伝わらない。バリアを `UserInformationException`（`RestorePriorityFileFailed`、ファイル名＋元のエラー、元の例外は InnerException）で fault させる | Win + Linux(WSL ext4): `RestoreFileFailureTests.AFailedPriorityFileStopsTheRestoreWithAnExplanation`（新規）直す前 赤（生の `UnauthorizedAccessException`）→ 直した後 緑。関連テスト 29 件が緑 | —（本文は `pr-drafts/restore-priority-file-failure-message.md`） |
| `fix/missing-destination-no-retry` | 3bfaae39 | **master 直上で独立**。`BackendManager` は全例外を再試行するので、保存先フォルダがない（`FolderMissingException`）restore も 5 回×10 秒 ≒ 51 秒待ってから失敗（実測）。フォルダを作るのはバックアップだけ（他の操作は `Controller` が autocreate を無効化）なので、バックアップ以外は即失敗。バックアップは共有が一時的に切れた場合もあるので従来どおり | Win + Linux(WSL ext4): `MissingDestinationRetryTests`（新規、`RetryList` を数える）restore は直す前 4 回 → 直した後 1 回。バックアップの番人は両方 4 回。関連 18 件が緑 | [#7368](https://github.com/duplicati/duplicati/pull/7368)（09-26、Issue なし。#4644 が触れている待ち時間で、#4644 本体は #7365。番人テストの別 DB は #7366 が入ったら戻せる） |
| `fix/local-database-open-failure-closes-connection` | 66cf22cb | **master 直上で独立**。`LocalDatabase.CreateLocalDatabaseAsync(path, ...)` は接続を開いてから初期化するが、初期化で例外が出ると接続を閉じない。初回バックアップの失敗時、`Controller` の失敗ハンドラが `operation: null` で開くと「直前の Operation がない」で例外になり、DB ファイルがプロセス終了まで開いたまま（Windows では削除できない）。例外時にコマンド・トランザクション・接続を解放してから rethrow する | Win + Linux(WSL ext4): `FailedOperationDatabaseTests`（新規。Windows は排他で開けるか、Linux は `/proc/self/fd` で確認）直す前 赤 → 直した後 緑。関連 16 件が緑 | [#7366](https://github.com/duplicati/duplicati/pull/7366)（09-26、Issue なし。4 番のブランチの番人テストは別 DB を使っているので、両方入ったら元に戻せる） |
| `fix/local-database-derived-setup-closes-connection` | 0de2de18 | **#7366（`66cf22cb`）の上に積んだ**（候補 3 番、Issue なし）。`LocalBackupDatabase.CreateAsync(path)` と `LocalDeleteDatabase.CreateAsync(path)`（compact・delete・purge）は、基底の作成の後に自分のコマンドを準備する。そこで失敗すると接続を閉じない。接続はトランザクションを持ったまま残るので、以後の書き込みは毎回 10 秒の busy 待ちになる。#7366 の解放処理を共通のメソッドに切り出し、2 か所でも使う | Win + Linux(WSL ext4): `FailedOperationDatabaseTests` に新規 2 件（準備するテーブルを消して Controller でバックアップ／コンパクト）。直す前 赤（ファイルが開いたまま、約 1.5 分）→ 直した後 緑（約 1 秒）。関連 13 件が緑。**中断での発生は Controller で 25 回試して 0 回（窓が狭い）** | —（#7366 のマージ後に rebase して出す。本文は `pr-drafts/local-database-derived-setup-closes-connection.md`） |
| `fix/http-report-status-result-errors` | 8372fe86 | **master 直上で独立**。`HttpReportStatus` の完了報告は例外の有無だけで決まり、結果にエラーがあっても（ファイルを戻せなかった restore など）`Completed`・`ErrorMessage` なし。#7365 と同じ穴の報告側。`ParsedResult` が Error/Fatal なら `ErrorMessage` を付ける（1 件ならそのエラー、複数なら `Got N error(s)`）。`Status` は `Completed` のまま。**あわせて** `Failed` 報告の例外メッセージもパスを伏せずに送っていた（実測）ので、どちらのエラーにもログ行と同じ規則でパス伏せ | Win + Linux(WSL ext4): `HttpReportStatusTests` に新規 5 件（本物の `RestoreResults` に `WriteMessage` でエラー/警告を載せる 3 件、失敗報告のパス 2 件）。直す前 3 赤＋番人 2 緑 → 直した後 5 緑。報告系 36 件が緑 | [#7367](https://github.com/duplicati/duplicati/pull/7367)（09-26、Issue なし。09-26 にパス伏せを含めて 1 コミットのまま更新） |
| `fix/live-log-poll-continues-from-oldest` | fea6b85d | **master 直上で独立**。#5572: ngclient は 3 秒ごとに `pagesize=100` で「見た最大 ID の続き」を取るが、`LogWriteHandler.AfterID` は 100 件を超えると**新しいほうの 100 件**を返していた（2018 年の `843b3e4e` から）→ 古いほうが二度と表示されない。続きの取得（`id > 0`）は古いほうから返し、最初の取得（`id <= 0`）は今どおり新しいほうを返す。ngclient の変更は不要 | Win: `LiveLogPollingTests`（新規、ngclient と同じ取り方で追い付くまで取る）。250 行のうち直す前 100 行（151-250）→ 直した後 250 行すべて。最初の取得の番人は両方緑。関連 API テスト 2 件が緑 | —（本文は `pr-drafts/live-log-poll-continues-from-oldest.md`、**`Fixes #5572`**） |
| `fix/repair-abort-keeps-database` | a4ce5c0e | **master 直上で独立**（Issue なし、候補 2 番）。`RepairHandler` は DB を読む際のあらゆる例外を「読めない」とみなし、DB を `.backup` にリネームして作り直す。中断によるキャンセルも同じ扱いだった → **修復の開始直後に中断すると、健全な DB が脇に移される**。Controller で 3 ms 刻みに中断すると 30 回中 9 回リネームしようとした。Linux では実際に 200 KB の DB が `.backup` に移り、新しい 4 KB の DB と、元の名前のまま取り残された WAL（457 KB）が残った。Windows は開いたままのファイルで Move が失敗して隠れる（#7366 が入ると成功するようになる）。キャンセル時は投げ直す | Win + Linux(WSL ext4): `RepairHandlerTests.AbortedRepairDoesNotSetTheDatabaseAsideAsync`（新規、中断の状態で `RunAsync`）直す前 赤 → 直した後 緑。Controller での中断は直した後 30 回中 0 回。`RepairHandlerTests` 23 件が緑。ロック中（busy）は Controller が先に失敗するので届かず、対象外 | —（本文は `pr-drafts/repair-abort-keeps-database.md`） |
| `fix/server-port-refused-tries-next` | a9a66e85 | **master 直上で独立**。#7330: 既定の `loopback` は Kestrel の `ListenLocalhost`（IPv4 と IPv6）で待ち受け、両方拒否されると `IOException` → `AggregateException` → `SocketException(10013)` ×2 になる。「次のポートへ」の判定がこの形を知らず、サーバーが落ちていた（トレイは 8200, 8300, 8400… を渡す）。判定を `IsPortUnavailable` に切り出し、中身が全部「使用中／拒否」の AggregateException も次へ進む | Win: 実際の Windows 除外範囲のポート（`netsh` で取得、管理者不要）、Linux(WSL): 特権ポート 1。`ServerMovesOnFromAPortItIsNotAllowedToUse_Async`（新規、実サーバー）直す前 赤（Issue と同じ例外で停止）→ 直した後 緑。`ServerApiIntegrationTests` 9 件が緑 | —（本文は `pr-drafts/server-port-refused-tries-next.md`、**`Fixes #7330`**） |
| `fix/queue-runner-dispose-before-terminate` | e263508b7c3b | **master 直上で独立**（Issue なし）。終了時、Web サーバーを止めると `App.RunAsync` が DI コンテナを破棄し、`QueueRunnerService.Dispose` が CTS を破棄する。後片付けの `Terminate` が破棄済みの CTS をキャンセルして `ObjectDisposedException` がログに出る（タイミング次第）。`Dispose` も終了扱いにしてキャンセルし、破棄済みのキャンセルは飛ばす | `QueueRunnerServiceShutdownTests`（新規）: 破棄→終了の順で直す前 赤（例外）→ 直した後 緑、逆順の番人は両方緑。`TaskQueueServiceTests` 緑。実サーバーの起動・停止では競合の再現が不安定（今回 6 回中 0 回、以前は 6 回中 2 回） | —（本文は `pr-drafts/queue-runner-dispose-before-terminate.md`） |
| `fix/sleep-prevention-dedicated-thread` | b4374c2c | **master 直上で独立**。#6837: Windows の `SetThreadExecutionState` の要求はスレッドごとで、同じスレッドが取り消すか終わるまで続く。`StartSleepPrevention` は `Task.Run` のループから 10 秒ごとに要求（＝スレッドプールのスレッド）、`StopSleepPrevention` は Dispose したスレッドでだけ取り消す → プールのスレッドの要求が残り、**バックアップが終わってもスリープしない**。専用スレッドが 1 回要求し（`ES_CONTINUOUS` で持続）、停止の合図で取り消してから終わる。macOS・Linux は変更なし | Win のみ（Windows 専用 API、Linux では Ignore）: `CallNtPowerInformation(SystemExecutionState)`（管理者不要）で実測、直す前は 操作前 `0x0` → 中 `0x1` → Dispose の 20 秒後も `0x1`（3 周とも）。`SleepPreventionTests`（新規、2 周）直す前 赤 → 直した後 緑。他のテストは `allow-sleep=true` なので経路に届かない | —（本文は `pr-drafts/sleep-prevention-dedicated-thread.md`、**`Fixes #6837`**） |
| `fix/restore-picker-unexpanded-root` | c19f74c3 | **master 直上で独立**（古い UI、JS のみ）。#3483: restore のツリーでチェックを付けると `toggleCheck` が親の子を調べるが、ルートは親がないので自分を親の代わりにし、子は展開するまで読み込まれていない → `p.children.length` で `TypeError`、何も選ばれない。ルートが 1 つなら自動で展開されるので隠れ、複数ドライブだと起きる。`p == node` ならループを飛ばす（直後の `p == node` でそのまま追加されるので結果は同じ） | Win、ブラウザで手動（JS のテストの仕組みはない）: `subst` で Q: と R: を作り 2 ドライブのバックアップ、`--webservice-webroot` をソースに向けたサーバー。直す前 `TypeError`（`restoreFilePicker.js:208`）で選択 `[]` → 直した後 `["Q:\srcA\"]`、エラーなし。2 つ目のチェック・解除、展開したルートと子も確認。展開前に選んだ 2 ルートを別フォルダに復元し、3 ファイルが中身ごと戻った | —（本文は `pr-drafts/restore-picker-unexpanded-root.md`、**`Fixes #3483`**） |
| `fix/list-prefix-multiple-roots` | 3ab6a0e5 | **master 直上で独立**（Issue なし、#3483 の再現中に発見）。複数ドライブ・UNC のバックアップでは `GetLargestPrefixAsync` がルートごとに自分を呼ぶが、179d1ceb（Async 化）以来、パスの読み取りを開いたまま呼ぶ → 内側の一時テーブルの `DROP` が SQLite のタイムアウト（30 秒）まで待つ（ダンプで確認）。さらにルートの後に `""` も返し、空のルートが余計に出る。パスを先にリストに読み、ルートを返したら終わる（同期版と同じ） | Win + Linux(WSL ext4): `ListPrefixMultipleRootsTests`（新規、DB のパスを `Q:\`・`R:\`・`\\server\share\` に書き換え）直す前 赤（余計な `""`、1 m 41 s / 1 m 40 s）→ 直した後 緑（11 s / 8 s）。API: 60.4 秒 → 0.47 秒、ルート 2 つだけ。**新しい UI**: 既定では v2 の `list-folder`（319 ms、空のルートなし）で**影響なし**。v2 を無効にすると `prefix-only` が 62.2 秒で空のルートも出た → 直した後 407 ms | —（本文は `pr-drafts/list-prefix-multiple-roots.md`） |
| `fix/s3-client-kept-on-edit` | 0b3af04c | **master 直上で独立**（古い UI、JS のみ）。#4405: 既存の S3 保存先を開くと、パーサーが URL の `s3-client` を読んだ後に、`$watch('Backend')` から S3 のローダーが走り、無条件に先頭の選択肢（aws）で上書きする → minio のジョブが aws と表示され、変更せずに保存しても `s3-client=aws` が書かれる。タイミングではなく、開くたびに起きる。ほかの既定値（サーバー・リージョン・ストレージクラス）と同じく、未設定のときだけ既定を入れる | Win、ブラウザで手動（JS のテストの仕組みはない）: `s3-client=minio` のジョブ（接続しないダミーの認証情報）を `--webservice-webroot` をソースに向けたサーバーで編集。直す前 aws 表示、変更なしで保存すると `s3-client=aws`（2 回再現）→ 直した後 minio 表示、保存後も `minio`。新規ジョブの既定は aws のまま、編集で aws に切り替えた保存も反映。**新しい UI**: 同じジョブを編集して保存しても `minio` のまま（影響なし、実測） | —（本文は `pr-drafts/s3-client-kept-on-edit.md`、**`Fixes #4405`**） |
| `fix/s3-custom-storage-class` | 261b64cf | **master 直上で独立**（古い UI、JS のみ。#4405 と同じ `EditUriBuiltins.js` の別の箇所、衝突なし）。#3750: 「Custom storage class」を選ぶと選択が null になり、入力値は別のフィールドに入る。URL の組み立ては選択が null でないときしかクラスを書かないので、入力値が捨てられて既定に戻る。すぐ上のリージョンと同じ `else if (custom != null)` を足す | Win、ブラウザで手動: `CUSTOM_TEST_CLASS`（サーバーの一覧にない値。`GLACIER_IR` は今は一覧にある）を入力して保存。直す前 URL に `s3-storage-class` なし → 直した後 `s3-storage-class=CUSTOM_TEST_CLASS`。開き直すと Custom＋入力値が表示され、変更なしの保存でも残る。一覧から Standard を選ぶと `STANDARD`（従来どおり）。**新しい UI**: 高度な設定の自由入力で保存すると `CUSTOM_TEST_CLASS` が入る（影響なし、実測） | —（本文は `pr-drafts/s3-custom-storage-class.md`、**`Fixes #3750`**） |
| `fix/local-folder-shown-on-edit` | 4ed8daeb | **master 直上で独立**（古い UI、JS＋テンプレート。`EditUriBuiltins.js` は #4405・#3750 と別の箇所）。#4001: `file.html` の `ng-init` が「パスがあれば文字入力、なければフォルダツリー」を読み込み時に 1 回だけ決める。ホームから編集を開くとテンプレートがキャッシュ済みで、ジョブの保存先より先に読み込まれる → パスが空と判断してツリーを表示（何も選ばれていない）。`file` のパーサーがパスを読んだときにも同じ判断をし、フラグを `Path` と同じエディタのスコープ（`$parent`）に置く。`ng-init` は新規・種類の切り替え用に残す | Win、ブラウザで手動: ホーム→編集で直す前 3/3 回ツリー表示（`Path` はあるのに `HideFolderBrowser` false）→ 直した後 4/4 回パスの文字入力を表示。Browse／Manually type path の切り替え、ツリーで選んでも表示が切り替わらないこと、変更なしの保存で URL が変わらないこと、新規ジョブはツリー、も確認。**新しい UI**: 直接開いてもホームから移っても保存先のパスが入力欄に出る（影響なし、実測） | —（本文は `pr-drafts/local-folder-shown-on-edit.md`、**`Fixes #4001`**） |

### 注意

- **一覧系 残り 4 本**（root-listing / list-folder-trailing-separator / search-folder-subfolders / list-folder-default-latest）は全部
  `DirectListHandlerTests.cs` にテストを足し、`LocalListDatabase.cs` / `ListFolderHandler.cs` の別の箇所を触る。意味的な衝突は無い。
  09-23 に先頭の #7326 がマージされた後の rebase では文面上の衝突も出なかったが、1 本マージされるごとに残りを rebase して確かめる
- ~~`fix/abort-unblocks-backend-call` は #6461 の**一部**~~ → #7325 として 09-23 にマージ済み。source ファイルの同期読み取りと SQLite 文の非 cancel 待ちは未対応のまま（#6461 は OPEN）
- ~~`fix/task-info-finished-current-task` も `Fixes #6597` は付けない~~ → [#7323](https://github.com/duplicati/duplicati/pull/7323) として 09-21 にマージ済み（`Fixes` 無しで出した。issue の race は再現できていない。再現できたのは「repair 失敗を UI が無視する」経路で、#6597 は OPEN のまま）

## マージ済み（表から外したもの）

| ブランチ | PR | マージ | 備考 |
|---|---|---|---|
| `fix/task-info-finished-current-task` | [#7323](https://github.com/duplicati/duplicati/pull/7323) | 2026-09-21、無改変（[`a2c14630`](https://github.com/duplicati/duplicati/commit/a2c14630ba60)） | `GET /api/v1/task/{id}` が終わったタスクを `Running` と答える窓。ngclient 側（別リポ `fix/restore-repair-failure`）の PR は利用者が別途。下書き [pr-drafts/task-info-finished-current-task.md](pr-drafts/task-info-finished-current-task.md) は残してある |
| `fix/purge-locks-strict-selection` | [#7343](https://github.com/duplicati/duplicati/pull/7343) | 2026-09-23、無改変（[`5977ba3b`](https://github.com/duplicati/duplicati/commit/5977ba3bb65f)） | 09-22 に足した「最初の fileset＋2 秒まで待つ」テスト修正込み |
| `fix/sync-mode-without-parameter` | [#7324](https://github.com/duplicati/duplicati/pull/7324) | 2026-09-23、無改変（[`f9103c5d`](https://github.com/duplicati/duplicati/commit/f9103c5d4c94)） | |
| `fix/list-file-versions-full-path` | [#7326](https://github.com/duplicati/duplicati/pull/7326) | 2026-09-23、無改変（[`4dbf01d7`](https://github.com/duplicati/duplicati/commit/4dbf01d74d56)） | 一覧系 5 本の先頭 |
| `fix/task-queue-waiting-status` | [#7341](https://github.com/duplicati/duplicati/pull/7341) | 2026-09-23、無改変（[`bcc5ae8e`](https://github.com/duplicati/duplicati/commit/bcc5ae8e0f15)） | |
| `fix/fileset-exact-match-no-fallback` | [#7342](https://github.com/duplicati/duplicati/pull/7342) | 2026-09-23、無改変（[`a8b0e218`](https://github.com/duplicati/duplicati/commit/a8b0e218ad4b)） | |
| `fix/abort-unblocks-backend-call` | [#7325](https://github.com/duplicati/duplicati/pull/7325) | 2026-09-23、無改変（[`d1fa7af3`](https://github.com/duplicati/duplicati/commit/d1fa7af394d6)） | マージ後にメンテナが `UntilCancelledAsync` を `Task.WaitAsync(token)` で書き直した（[`8d866951`](https://github.com/duplicati/duplicati/commit/8d866951d94a)、#7355 内）。読んだ限り挙動は同じ（待ちを抜けたら放棄した呼び出しの例外を観測する）、実行では確かめていない |
| `fix/restore-test-corrupt-data-volume` | — | **不要になった**（PR 未作成） | メンテナの [#7355](https://github.com/duplicati/duplicati/pull/7355)（[`2886bdac`](https://github.com/duplicati/duplicati/commit/2886bdacca1e)）が同じフレークを同じ方法（DB からデータを持つ dblock を選ぶ）で直した。向こうは「ファイル数が最多の volume」を選び、同じ癖を持つ 2 か所目（`dblocks[0]` を壊す別テスト）も直している。rebase で conflict になったのでローカルは abandon、origin のブランチも 09-23 に削除（中身は `5ac801de`） |
| `fix/restore-cancel-late-block-waiters` | [#7357](https://github.com/duplicati/duplicati/pull/7357) | 2026-09-25、無改変（[`8149b181`](https://github.com/duplicati/duplicati/commit/8149b1818b9d)） | 壊れた volume で restore が永久に止まる件。穴 5 つ（`CancelAll` 後の待ち手、止まった段の上流の書き手 4 か所）。09-24 に下流 3 か所を足した版がそのまま入った |
| `fix/list-folder-default-latest` | [#7347](https://github.com/duplicati/duplicati/pull/7347) | 2026-09-25、無改変（[`9002e211`](https://github.com/duplicati/duplicati/commit/9002e211047f)） | 一覧系の 1 本目。これで #7345 が `DirectListHandlerTests.cs` で conflict（同じ位置に両方がテストを追加）→ 両方残して解消 |
| `fix/search-folder-subfolders` | [#7346](https://github.com/duplicati/duplicati/pull/7346) | 2026-09-25、無改変（[`df50511d`](https://github.com/duplicati/duplicati/commit/df50511dd1a7)） | 一覧系の 2 本目。この後の rebase で残り 2 本と #7336 に conflict なし |
| `fix/list-folder-trailing-separator` | [#7345](https://github.com/duplicati/duplicati/pull/7345) | 2026-09-25、無改変（[`348bd479`](https://github.com/duplicati/duplicati/commit/348bd47942f2)） | 一覧系の 3 本目。#7347 との conflict を解消した版がそのまま入った |
| `feature/hyperv-mssql-source-providers` | [#7336](https://github.com/duplicati/duplicati/pull/7336) | 2026-09-25 **CLOSED**（マージではない） | メンテナが**このコミットを自分の [#7274](https://github.com/duplicati/duplicati/pull/7274) に取り込み**（そのブランチに rebase、作者は利用者のまま）、#7274 に一本化するため閉じた。コメント：「rebase されていたので綺麗にはマージできなかったが cherry-pick した」。こちらの対応は不要 |

## 見つけたが直していないもの（同じ調査で）

- `search` の help が `--version` で「無ければ最新（version=0）」、同じブロックの `--all-versions` で「全版が既定」と言っていて矛盾。実装は後者（実測：版指定なしで versions=1,0,0 が返る）

- ~~`TaskQueueService.GetTaskQueue` が待機中タスクを `Running` と報告~~ → `fix/task-queue-waiting-status` で修正
- `QueueRunnerService` の `CachedTaskResult.TaskFinished` が `DateTime.Now`（ローカル）、`TaskStarted` が `UtcNow`。JSON はオフセット付きで出るので瞬間は正しい
- ~~`SetLocksHandler` の「No version specified」が「指定はあったが一致しない」場合にも出る~~ → `fix/purge-locks-strict-selection` で修正
- `RecordSyncOperation` が `Operation` 表に `Rsync N` 行を入れ、`Controller` の後始末が最新 Operation 行に相乗りする → バックアップのログが sync の operation id に紐づく可能性。`LogData.OperationID` を join して読む箇所が見つからず症状を示せない
- `ShouldTriggerSync` は `m_dbpath` を検査しない（`RecordSyncOperation` はする）。backup では dbpath が常に有るので実害なし

## 使い捨ての再現テスト（scratchpad、セッション e8cd3eb2 が消えたら消える）

`ProbeListFileVersions.cs`、`ProbeListFolder.cs`、`ProbeSearchFolder.cs` — Controller 経由で症状を `TestContext.Progress` に出すだけの
テスト。同じ内容は各ブランチのテストに正式に入っているので、無くなっても困らない。
